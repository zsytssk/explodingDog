import { BaseCtrl } from "../../../mcTree/ctrl/base";

export class ExplodePosCtrl {
    private view;
    constructor(view) {
        this.view = view;
    }
    init() {
        const { btnList, btnSelect } = this.view;
        // btnList.
    }
}